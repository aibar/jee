language: java
jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.m2

services:
  - postgresql

script:
  - mvn test -B -Ddb=hsql
  - mvn test -B -Ddb=pg

deploy:
  provider: script
  script: mvn deploy -s settings.xml -B -Dbintray=$bintray -DskipTests
  on:
    tags: true